/** @deprecated Prefer `@repo/report-engine` — kept as thin re-export for existing imports. */
export {
  classificationFromTriangulation,
  evaluateWizardTriangulation,
} from "@repo/report-engine";
