type BasicCourseLinkPanelProps = {
  variant?: "inline" | "card";
  className?: string;
};

/** Ссылки на курсы / Яндекс.Диск отключены — панель не рендерится. */
export function BasicCourseLinkPanel(_props: BasicCourseLinkPanelProps) {
  return null;
}
