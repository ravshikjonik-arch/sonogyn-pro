type FetalAnatomyEducationalLinkPanelProps = {
  variant?: "inline" | "card";
  className?: string;
};

/** Ссылки на учебные курсы ISUOG отключены — панель не рендерится. */
export function FetalAnatomyEducationalLinkPanel(_props: FetalAnatomyEducationalLinkPanelProps) {
  return null;
}
