{
  "name": "@repo/examination-engine",
  "version": "0.1.0",
  "private": true,
  "description": "ExamEngine — timed/quick MCQ + image-Q lifecycle over @repo/education-quiz (no UI).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "sideEffects": false,
  "dependencies": {
    "@repo/education-quiz": "workspace:*",
    "zod": "^3.24.4"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"skip\"",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.4",
    "typescript": "^5.9.2"
  }
}
