/** Заготовка: перенос ProceduralUterus из @clinical/uterus — см. README. */
export type UterusModelProps = {
  /** Идентификатор профиля матки (норма / увеличенная / post-C/S). */
  profileId?: string;
};

export function UterusModel(_props: UterusModelProps) {
  return null;
}
