{
  "name": "@repo/education-quiz",
  "version": "0.1.0",
  "private": true,
  "description": "Shared QuizBank contracts, Zod schemas, and pure MCQ helpers (no UI).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts"
  },
  "sideEffects": false,
  "dependencies": {
    "zod": "^3.24.4"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"skip\"",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.4",
    "typescript": "^5.9.2"
  }
}
