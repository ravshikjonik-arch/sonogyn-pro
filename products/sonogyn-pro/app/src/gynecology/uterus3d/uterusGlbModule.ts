/**
 * После добавления файла `assets/models/uterus.glb` укажите модуль так:
 *
 * `export const UTERUS_GLB_MODULE = require("../../assets/models/uterus.glb");`
 *
 * Metro упакует бинарник; URI будет резолвиться через expo-asset на устройстве.
 */
export const UTERUS_GLB_MODULE: number | null = null;
